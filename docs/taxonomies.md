# taxonomies.yaml


Defines taxonomies and their URL slug aliases.


```yaml
types:
  - series
  - tags
 
aliases:
  categories:
    社会: shakai
  series:
    Ruby入門: ruby-intro
```


Both `types` and `aliases` are optional.
